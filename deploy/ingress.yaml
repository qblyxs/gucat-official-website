apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev.gucat.vip
  namespace: gucat-test
spec:
  ingressClassName: nginx
  rules:
    - host: dev.gucat.vip
      http:
        paths:
          - backend:
              service:
                name: gucat-web-service
                port:
                  number: 4000
            path: /
            pathType: Prefix