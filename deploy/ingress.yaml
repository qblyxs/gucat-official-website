apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev.gucat.vip
  namespace: gucat-dev
spec:
  ingressClassName: nginx
  rules:
    - host: dev.gucat.vip
      http:
        paths:
          - backend:
              service:
                name: gucat-web-service
                port:
                  number: 80
            path: /
            pathType: Prefix
    - host: dev.gucat.vip
      http:
        paths:
          - backend:
              service:
                name: gucat-web-service
                port:
                  number: 80
            path: /
            pathType: Prefix